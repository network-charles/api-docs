---
title: Create Financial Portfolio
---

The Create Financial Portfolio endpoint allows you to create a new portfolio with a specified name and initial balance.

## Endpoint

---

- `POST` - /financial-portfolio

## Example

{/* code begins */}
import { Code } from 'astro-expressive-code/components';
import { Tabs, TabItem } from '@astrojs/starlight/components';
import codeOne from 'post/post-one.bash?raw';
import codeTwo from 'post/post-two.py?raw';
import codeThree from 'post/post-three.yml?raw';

<Tabs>
  <TabItem label="Bash">
    <Code code={codeOne} lang="bash" />
  </TabItem>
  <TabItem label="Python">
    <Code code={codeTwo} lang="python" />
  </TabItem>
  <TabItem label="YAML">
    <Code code={codeThree} lang="yaml" />
  </TabItem>
</Tabs>
{/* code ends */}

## Request

---

This endpoint contains the following arguments:

**Headers**

- `x-rapidapi-key` (string, required): Your API key
- `x-rapidapi-host` (string, required): The server URL, excluding the protocol part of the URL, "https://".

**Parameters**

- `portfolio_name` (string, required): The name of the financial portfolio.
- `initial_balance` (float, required): The initial balance of the portfolio.

## Response

---

The response includes the following attributes, which provide details about the data returned.

**Metadata**

- `status` (string): Indicates the current status of the API response. Values include include "success" or "error".

- `message` (string): A human-readable message indicating the result of the request.

**Response data**

The `portfolio` object includes the following response attributes:

---

- `portfolio_id` (string): A unique identifier for the portfolio.
- `portfolio_name` (string): The name of the financial portfolio.
- `initial_balance` (float): The initial balance of the portfolio.
- `creation_time` (string): The time when the portfolio was created in UTC.
